
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(qtun)

INCLUDE_DIRECTORIES(../lib)

FILE(GLOB HDRS_LIST "*.h")
AUX_SOURCE_DIRECTORY(. SRCS_LIST)

ADD_EXECUTABLE(qtun ${HDRS_LIST} ${SRCS_LIST})
TARGET_LINK_LIBRARIES(qtun qvn)
