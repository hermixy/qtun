
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)

PROJECT(qvn)

INCLUDE(${CMAKE_CURRENT_SOURCE_DIR}/../warning2error.cmake)

FILE(GLOB HDRS_LIST "*.h")
AUX_SOURCE_DIRECTORY(. SRCS_LIST)

ADD_LIBRARY(qvn ${HDRS_LIST} ${SRCS_LIST})
